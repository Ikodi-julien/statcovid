version: "3.8"
services:
  php:
    build: .
    image: ikodi/statcovid
    container_name: covid_symfony
    ports:
      - 5656:80
    environment:
      # - APP_ENV=prod
      - APP_DEBUG=0
    volumes:
      - ./vhosts:/etc/apache2/sites-enabled
      - ./project:/var/www/project
